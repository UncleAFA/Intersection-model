namespace Intersection_model
{
    public class bibl
    {
        public enum Load
        {
            HIGH, MEDIUM, LOW
        };
        public enum Direction
        {
            LEFT, RIGHT, STRAIGHT, BACK
        };
        public enum CarType
        {
            ACar, Truck
        };
        public enum Color
        {
            GREEN, YELLOW, RED
        };
    }
}
